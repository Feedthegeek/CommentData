from setuptools import setup

setup(
    name='CommentData',
    version='0.1.0',
    packages=[''],
    install_requires=['os', 'praw', 'pytz', 'datetime', 'typing'],
    author='Nicholas D Herrick',
    author_email='herricknd@icloud.com'
)